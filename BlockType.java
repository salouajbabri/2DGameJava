
public enum BlockType {
	castle(), // the castle block
	dirt(), // the plain dirt block
	dirtGrass(), // the dirt block with grass on top
	empty(), // an empty transparent block
	outsidebg(), // an outside block that matches the outsidebg color
	castlebg(), // a castlegb block that matches the color of castlebg
	lavaTop(), // lavaTop
	win(), // win block
	lava(); // normal lava block
}
